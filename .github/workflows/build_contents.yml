name: build_contents

on:
  push:
    branches: 
      - main
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y z80asm make

      - name: Run Makefile
        working-directory: ./roms
        run: make

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: contents.zip
          path: |
            roms/res/contents.bin
            roms/res/contents.afp
            roms/res/BASICNL1.1.afp
            roms/res/Forth.afp
            roms/res/UCSD-Pascal.afp
            roms/res/assembler-5.9.afp
            roms/res/JWSBasic.afp
            roms/res/familiegeheugen-4.afp
            roms/res/Maintenance-2.afp
